# Runtime State

Current phase:

Bootstrap + MCP foundation

Current objective:

Create first runnable local runtime capable of exposing tools.

Next milestones:

- Hook runtime to MCP layer
- Add service mode
- Add installer pipeline
- Add tool call routing
